info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: "{{BASE_URL}}/register/"
  body:
    type: json
    data: '{ "username": "testuser2", "email": "test2@example.com", "name": "Test User", "password": "12345678" }'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: "{{PROD_URL}}/register/"
  body:
    type: json
    data: '{ "username": "testuser", "email": "test@example.com", "name": "Test User", "password": "12345678" }'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

info:
  name: ME
  type: http
  seq: 4

http:
  method: GET
  url: "{{BASE_URL}}/me/"
  headers:
    - name: Authorization
      value: Token {{token_1}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

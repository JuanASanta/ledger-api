info:
  name: Retrieve Category
  type: http
  seq: 3

http:
  method: GET
  url: "{{BASE_URL}}/categories/{{CATEGORY_1_ID}}/"
  headers:
    - name: Authorization
      value: Token {{token_1}}
    - name: Authorization
      value: Token {{token_2}}
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
